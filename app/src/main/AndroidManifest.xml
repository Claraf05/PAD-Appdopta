<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.ucm.fdi.appdopta">

    <application
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:banner="@mipmap/logo"
        android:logo="@mipmap/logo"
        android:icon="@mipmap/logo"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity
            android:name=".features.user.changeInfo.ChangePasswordActivity"
            android:exported="false" />
        <activity
            android:name=".features.user.changeInfo.ChangeUserActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".features.user.UserActivity" />
        <activity
            android:name=".features.settings.SettingsActivity"
            android:label="@string/setting" />
        <activity android:name=".features.login.LoginActivity" />
        <activity android:name=".features.register.RegisterActivity" />
        <activity android:name=".features.Fichas.fichaActivity" />

        <activity android:name=".features.Fichas.aniadirFichaActivity"/>
        <activity android:name=".PrincipalView"/>
        <activity android:name=".FiltroView"/>
        <!--</activity>-->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>